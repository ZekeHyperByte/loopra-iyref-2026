import { RoleDashboardHome } from "./RoleDashboardHome";

/** Role-specific home: enterprise supply metrics vs admin operations metrics. */
const DashboardIndex = () => <RoleDashboardHome />;

export default DashboardIndex;
